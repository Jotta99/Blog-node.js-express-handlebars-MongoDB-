{{msg_success}}
        {{>_msg}}

{{#each erros}}
    <div class="alert alert-danger" role="alert">{{texto}}</div>
{{else}}

{{/each}}

<div class="card mt-4 shadow-sm p-3 mb-2 bg-body-tertiary rounded" style="margin-top: 8rem !important;">
    <div class="card-body">
        <h2>Criar Conta</h2>
        <form action="/usuarios/registro/novo" method="POST">
            <div class="form-group mt-2">
                <label for="nome">Nome</label>
                <input type="text" class="form-control" id="nome" name="nome" placeholder="Nome">
            </div>
            <div class="form-group mt-2">
                <label for="email">Endereço de Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Preencha seu Email">
            </div>
            <div class="form-group mt-2">
                <label for="nick">Nickname (Apelido)</label>
                <input type="text" class="form-control" id="nick" name="nick" placeholder="Como gostaria de ser chamado?">
            </div>
            <div class="form-group mt-2">
                <label for="senha ">Senha</label>
                <input type="password" class="form-control" id="senha" name="senha" placeholder="Senha">
            </div>
            <div class="form-group mt-2">
                <label for="senha2">Confirme a senha</label>
                <input type="password" class="form-control" id="senha2" name="senha2" placeholder="Confirme a sua senha">
            </div>
                <small id="info" class="form-text text-muted">Suas informações não serão compartilhadas.</small>
                <br>
            <button type="submit" class="btn btn-dark mt-2">Cadastrar</button>
        </form>
    </div>
</div>